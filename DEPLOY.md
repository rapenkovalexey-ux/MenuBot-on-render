# 🚀 Деплой бота на Koyeb + Turso

## Что изменилось в коде

| Файл | Изменение |
|---|---|
| `requirements.txt` | Добавлены `libsql` и `sqlalchemy-libsql` (актуальные пакеты Turso 2025) |
| `config.py` | Добавлены `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`, флаг `USE_TURSO` |
| `database/db.py` | SQLAlchemy engine автоматически переключается: Turso в продакшне, SQLite локально |
| `.env.example` | Обновлён под новые переменные |

---

## ШАГ 1 — Создать базу данных в Turso

### Через CLI (быстрее, рекомендуется)

**Установка Turso CLI:**

```bash
# macOS / Linux
curl -sSfL https://get.turso.tech/install.sh | bash

# Windows (в PowerShell)
winget install turso
```

**Создание базы:**

```bash
# Войти через GitHub аккаунт (откроется браузер)
turso auth login

# Создать базу данных (имя любое, латиницей)
turso db create menu-bot

# Получить URL базы — скопируйте строку вида:
# libsql://menu-bot-ВАШ_ЛОГИН.turso.io
turso db show menu-bot --url

# Создать токен доступа — скопируйте длинную строку JWT
turso db tokens create menu-bot
```

### Через сайт (без командной строки)

1. Зайдите на [app.turso.tech](https://app.turso.tech) → Sign Up через GitHub
2. Нажмите **Create database**
3. Имя: `menu-bot`, регион: **Frankfurt (fra)** — ближайший к России
4. После создания откройте БД → вкладка **Connect**
5. Скопируйте **Database URL** и нажмите **Generate Token** → скопируйте токен

---

## ШАГ 2 — Загрузить код на GitHub

1. Зайдите на [github.com](https://github.com) → **New repository**
2. Имя: `menu-bot`, видимость: **Private**
3. Нажмите **Create repository**

Загрузите все файлы из этой папки **через браузер** (кнопка "uploading an existing file") или через Git:

```bash
git init
git add .
git commit -m "Initial commit: menu bot"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/menu-bot.git
git push -u origin main
```

> ⚠️ Убедитесь что файл `.env` **НЕ попал** в репозиторий (он в `.gitignore`)

---

## ШАГ 3 — Зарегистрироваться на Koyeb

1. Откройте [koyeb.com](https://koyeb.com) → **Sign up**
2. Выберите **Continue with GitHub** — это самый быстрый способ
3. Подтвердите email если потребуется

---

## ШАГ 4 — Создать сервис на Koyeb

1. В дашборде нажмите **Create App**
2. Выберите источник: **GitHub**
3. Нажмите **Install GitHub App** → разрешите доступ к репозиторию `menu-bot`
4. Выберите репозиторий `menu-bot`, ветка `main`

**Настройки сервиса:**

| Параметр | Что выбрать |
|---|---|
| Service type | **Worker** ← ВАЖНО, не Web! |
| Builder | **Buildpack** |
| Run command | `python main.py` |
| Instance | **Free** |
| Region | **Frankfurt** (ближе к пользователям) |

---

## ШАГ 5 — Переменные окружения

В разделе **Environment variables** добавьте каждую переменную через кнопку **Add variable**:

| Имя переменной | Значение |
|---|---|
| `BOT_TOKEN` | Токен от @BotFather |
| `GROQ_API_KEY` | Ключ с console.groq.com |
| `TURSO_DATABASE_URL` | `libsql://menu-bot-ВАШ_ЛОГИН.turso.io` |
| `TURSO_AUTH_TOKEN` | Длинный JWT-токен от Turso |
| `SUPPORT_EMAIL` | Ваш email |

> 💡 Переменные `SMTP_*` добавьте только если хотите работающую отправку email в поддержку

---

## ШАГ 6 — Деплой

Нажмите **Deploy** в правом верхнем углу.

Koyeb установит зависимости и запустит бота за 3–7 минут.

**Как проверить:** откройте вашего бота в Telegram и напишите `/start`. Если ответил — всё работает.

**Если не работает:** Koyeb → ваш сервис → вкладка **Deployments** → нажмите на деплой → **Logs**.

---

## Локальная разработка (на вашем компьютере)

Создайте файл `.env` в папке проекта:

```env
BOT_TOKEN=ваш_токен
GROQ_API_KEY=ваш_ключ
# Turso не указываем — бот будет использовать локальный SQLite файл
```

Установите зависимости и запустите:

```bash
pip install -r requirements.txt
python main.py
```

Файл базы данных `menu_bot.db` создастся автоматически рядом с `main.py`.

---

## Часто задаваемые вопросы

**Q: Бесплатный тариф Turso — какие лимиты?**
100 баз данных, 5 ГБ хранилища, 500 млн строк чтений и 10 млн строк записей в месяц. Для Telegram-бота с тысячами пользователей — более чем достаточно. Карта не нужна.

**Q: Бесплатный тариф Koyeb — какие лимиты?**
1 Worker-сервис, 512 МБ RAM, 0.1 vCPU. Бот работает без засыпания. Карта не нужна.

**Q: Пакет `sqlalchemy-libsql` не устарел?**
Нет. Это актуальный пакет от команды Turso, выпущенный в июне 2025. Старые пакеты `libsql-experimental` и `libsql-client` — устарели и не поддерживаются.

**Q: Что если бот работает локально, но падает на Koyeb?**
Проверьте Logs в дашборде Koyeb. Чаще всего причина — неправильно скопированный токен (лишние пробелы) или опечатка в URL Turso.

**Q: Как просмотреть данные в базе Turso?**
```bash
turso db shell menu-bot
# Откроется SQL-консоль, введите:
SELECT * FROM users;
```
Или через веб: app.turso.tech → ваша БД → **Edit Data**.
