# 🚀 Деплой на Render.com + Turso
# Инструкция для мобильного браузера (Android)

---

## ШАГ 1 — Создать базу данных Turso

1. Откройте в браузере: **app.turso.tech**
2. Нажмите **Sign Up**
3. Выберите **Continue with GitHub**
   - Если нет GitHub — сначала зарегистрируйтесь на **github.com**
4. После входа нажмите кнопку **Create database**
5. Заполните:
   - Name: `menu-bot`
   - Region: **Frankfurt (fra)**
6. Нажмите **Create**
7. Нажмите на созданную БД `menu-bot`
8. Откройте вкладку **Connect**
9. Скопируйте строку **Database URL**
   - Выглядит так: `libsql://menu-bot-ВАШЕ_ИМЯ.turso.io`
   - Сохраните в заметки телефона
10. Нажмите **Generate Token**
    - Скопируйте длинную строку токена
    - ⚠️ Сохраните сразу — показывается только один раз!
    - Сохраните в заметки телефона

---

## ШАГ 2 — Загрузить код на GitHub

1. Откройте **github.com** → войдите в аккаунт
2. Нажмите **+** (иконка плюса вверху) → **New repository**
3. Заполните:
   - Repository name: `menu-bot`
   - Visibility: **Private**
4. Нажмите **Create repository**
5. На странице репозитория найдите ссылку:
   **"uploading an existing file"** — нажмите на неё
6. Нажмите **choose your files**
7. В файловом менеджере телефона выберите ВСЕ файлы из
   распакованной папки `bot_render`:
   - main.py
   - config.py
   - requirements.txt
   - render.yaml
   - Procfile
   - .env.example
   - .gitignore
   - И все папки: handlers/, database/, services/, keyboards/, assets/
8. Нажмите **Commit changes** (зелёная кнопка внизу)

> ⚠️ Файл `.env` НЕ загружайте — его нет в архиве, это правильно

---

## ШАГ 3 — Зарегистрироваться на Render

1. Откройте **render.com**
2. Нажмите **Get Started**
3. Выберите **GitHub** → разрешите доступ
4. Подтвердите email если придёт письмо

---

## ШАГ 4 — Создать сервис на Render

1. В дашборде нажмите **New +**
2. Выберите **Background Worker**
   ⚠️ Именно Background Worker — не Web Service!
3. Выберите **GitHub**
4. Найдите репозиторий `menu-bot` → нажмите **Connect**
5. Заполните настройки:
   - Name: `menu-bot` (любое)
   - Region: **Frankfurt (EU)**
   - Branch: `main`
   - Runtime: **Python 3**
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`
   - Instance Type: **Free**
6. Пролистайте вниз до раздела **Environment Variables**

---

## ШАГ 5 — Добавить переменные окружения

Нажимайте **Add Environment Variable** и добавляйте по одной:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | Токен от @BotFather |
| `GROQ_API_KEY` | Ключ с console.groq.com |
| `TURSO_DATABASE_URL` | `libsql://menu-bot-ВАШ_ЛОГИН.turso.io` |
| `TURSO_AUTH_TOKEN` | Длинный токен от Turso |
| `SUPPORT_EMAIL` | Ваш email |

---

## ШАГ 6 — Запустить деплой

1. Нажмите **Create Background Worker**
2. Render начнёт сборку — займёт 3–5 минут
3. В разделе **Logs** увидите процесс установки пакетов
4. Когда появится строка `Bot started!` — бот запущен ✅

---

## ШАГ 7 — Проверить

Откройте Telegram → найдите своего бота → напишите `/start`

---

## ⚠️ Важно про засыпание

Render на бесплатном тарифе **усыпляет сервис** после 15 минут
без активности. При первом сообщении после паузы бот
просыпается ~30–50 секунд.

Это нормально для тестирования. Для постоянной работы
можно будет перейти на платный тариф ($7/мес) или переехать
на другую платформу.

---

## Если что-то пошло не так

Render → ваш сервис → вкладка **Logs** — там будет ошибка.

Частые проблемы:
- **ModuleNotFoundError** → проверьте что requirements.txt загружен на GitHub
- **Бот не отвечает** → проверьте BOT_TOKEN (нет лишних пробелов?)
- **Ошибка Turso** → TURSO_DATABASE_URL должен начинаться с `libsql://`
